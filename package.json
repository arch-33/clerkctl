{
  "name": "clerk-utils",
  "author": "",
  "description": "Clerk utilities CLI",
  "license": "MIT",
  "type": "module",
  "version": "0.0.0",
  "files": [
    "dist"
  ],
  "bin": {
    "clerk-utils": "dist/cli.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prebuild": "tsc -p src/tsconfig.json",
    "build": "tsup --silent",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint-staged": "lint-staged",
    "prepublishOnly": "npm run build",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,cts,mts,json,md,mdx,yml,yaml}": "prettier --write --ignore-unknown"
  },
  "tsup": {
    "entry": [
      "src/bin/cli.ts"
    ],
    "format": [
      "esm"
    ],
    "tsconfig": "src/tsconfig.json",
    "clean": true,
    "splitting": true,
    "minify": true
  },
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "commander": "^14.0.3",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/node": "22.x",
    "husky": "^9.0.0",
    "lint-staged": "^16.3.2",
    "prettier": "^3.3.3",
    "tsup": "^8.5.1",
    "typescript": "5.9.3"
  }
}
